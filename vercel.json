{
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://shikharji-api.vercel.app/:path*", "method": ["GET", "POST", "PUT", "DELETE"] }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
